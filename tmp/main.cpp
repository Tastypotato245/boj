#include <iostream>

using namespace std;

int	N;

void Input()
{
	int	i = 0;
	cin >> N;
	//while (++i <= N) cin >> Arr[i];
}

int	main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	return (0);
}
